
<div class="row">
  <div class="col-auto">
    <h2>The Tech Blog Comments</h2>
  </div>
</div>

{{#each projects as |project| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>

      <!-- Call helper  -->

      {{{get_emoji}}}
      <a href="/project/{{project.id}}">{{project.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="star">⭐</span>
      <span class="dollar-amount">{{format_amount project.needed_funding}}</span>
        <div class="col-md-7">
    <p>
      {{project.description}}
    </p>
  </div>
    </p>
    <p>Posted by {{project.user.name}} on {{format_date project.date_created}}
        <div class="col-md-6">
        <button class="btn btn-sm btn-primary"> Comment </button>
      </div>
    </p>
  </div>
</div>
{{/each}}
